services:
  server:
    container_name: vigil-server
    image: ghcr.io/pineappledr/vigil:latest
    ports:
      - "8090:8090"
    volumes:
      - vigil_data:/data
    restart: unless-stopped
    environment:
      - PORT=8090
      - DB_PATH=/data/vigil.db
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:8090/health"]
      interval: 30s
      timeout: 3s
      retries: 3
      start_period: 5s

  # Uncomment below to run agent alongside server
  # agent:
  #   image: ghcr.io/pineappledr/vigil-agent:latest
  #   container_name: vigil-agent
  #   network_mode: host
  #   privileged: true
  #   volumes:
  #     - /dev:/dev:ro
  #   restart: unless-stopped
  #   command: ["--server", "http://localhost:8090", "--interval", "60"]
  #   depends_on:
  #     server:
  #       condition: service_healthy

volumes:
  vigil_data:
